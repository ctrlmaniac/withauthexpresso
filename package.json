{
  "name": "root",
  "private": true,
  "workspaces": [
    "app/server"
  ],
  "scripts": {
    "watch:server": "npx lerna exec --scope=server npm run watch"
  },
  "devDependencies": {
    "eslint": "^8.51.0",
    "lerna": "^7.3.0"
  }
}
